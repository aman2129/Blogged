class BookmarkDataModel {
  final String imageUrl, title, desc, userId, id;

  BookmarkDataModel({
    required this.imageUrl,
    required this.title,
    required this.desc,
    required this.userId,
    required this.id,
  });
}
